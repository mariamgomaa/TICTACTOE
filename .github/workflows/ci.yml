name: TICTACTOE CI/CD

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '5.15.2'
        arch: 'win64_mingw81'

    - name: Download GoogleTest
      run: |
        git clone https://github.com/google/googletest.git
        
    - name: Build main project
      run: |
        qmake TicTacToe.pro
        mingw32-make

    - name: Build and run tests (with verbose output)
      run: |
        qmake test_tictactoe.pro
        mingw32-make VERBOSE=1 2>&1 | tee build.log
        if ($LASTEXITCODE -ne 0) {
          echo "Build failed. Last 50 lines of output:"
          Get-Content build.log | Select-Object -Last 50
          exit 1
        }
        ./release/test_tictactoe.exe --gtest_color=yes


    - name: Deploy Qt runtime
      run: |
        windeployqt release/TicTacToe.exe

    - name: Upload built executable
      uses: actions/upload-artifact@v4
      with:
        name: TicTacToeGame
        path: release/**
