name: TICTACTOE CI/CD

on:
  push:
  pull_request:

permissions:
  contents: write  # ✅ Required for creating GitHub releases

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '5.15.2'
        arch: 'win64_mingw81'

    - name: Build project
      run: |
        qmake TicTacToe.pro
        mingw32-make

    - name: Deploy Qt runtime
      run: |
        windeployqt release/TicTacToe.exe

    - name: List built files
      run: Get-ChildItem -Recurse -File | ForEach-Object { $_.FullName }

    - name: Upload built executable as artifact
      uses: actions/upload-artifact@v4
      with:
        name: TicTacToeGame
        path: release/**

    - name: Upload to GitHub Release (on tag push only)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        name: "TicTacToe ${{ github.ref_name }}"
        tag_name: ${{ github.ref_name }}
        files: release/TicTacToe.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

